[req]
distinguished_name = req_distinguished_name
req_extensions     = v3_req
prompt             = no

[req_distinguished_name]
CN = channelog-service.channelog.svc

[v3_req]
subjectAltName = @alt_names

[alt_names]
DNS.1 = channelog-service.channelog.svc
DNS.2 = channelog-service.channelog.svc.cluster.local
